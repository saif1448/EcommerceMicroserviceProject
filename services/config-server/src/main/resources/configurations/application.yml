eureka:
  instance:
    hostname: localhost
  client:
    service-url:
      defaultZone: http://localhost:9000/eureka

spring:
  cloud:
    config:
      override-system-properties: false
  zipkin:
    base-url: http://localhost:9411/

management:
  tracing:
    sampling:
      probability: 1.0